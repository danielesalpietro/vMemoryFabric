[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --tb=short -q
markers =
    gpu: tests that require real GPU hardware (CUDA) to run
    avx512: tests whose result is only meaningful with real AVX-512 hardware (issue #33 Fase 1)
