# OSX-PoC — vLLM extras (Sprint 3)
#
# NOT installed by the base Dockerfile / docker-compose build. This file is a
# placeholder until the GCSG hooks into vLLM internals (currently
# NotImplementedError stubs) are actually implemented.
#
# Status as of 2026-08-07: vllm==0.4.3 does NOT currently install — it pins
# vllm-flash-attn==2.5.8.post2, which has been removed from PyPI (only 2.6.1
# and 2.6.2 remain). Do not just bump the version blindly: vLLM 0.4.3 was
# chosen because the hooks target its internal API, so re-pin only once the
# hook code exists and you can verify which vLLM version it actually needs.
#
# Install manually once ready:
#   pip install -r requirements-vllm.txt

vllm==0.4.3
